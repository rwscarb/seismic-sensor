services:
  seismic-sensor:
    build: .
    image: seismic-sensor:latest
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
