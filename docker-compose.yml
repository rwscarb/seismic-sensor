services:
  seismic-sensor:
    build: .
    image: seismic-sensor:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./checkpoints:/checkpoints:ro
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
